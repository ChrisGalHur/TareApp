<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="FullScreenDialog" parent="Theme.AppCompat.Dialog">
        <item name="android:windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowIsFloating">false</item>
        <item name="android:windowContentOverlay">@null</item>
        <item name="android:windowFullscreen">true</item>
        <item name="android:windowIsTranslucent">true</item>
    </style>

    <!-- Splash screen theme -->
    <style name="Theme.Tareapp.Splash" parent="Theme.MaterialComponents.DayNight">
        <item name="android:windowBackground">@color/medium_blue</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowFullscreen">true</item>
    </style>

    <!-- Tema personalizado para el TimePicker -->
    <style name="CustomTimePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/dark_blue</item>
        <item name="android:textColorPrimary">@color/light_blue</item>
        <item name="android:textColorSecondary">@color/medium_blue</item>
    </style>

    <!-- Tema personalizado para el DatePicker -->
    <style name="CustomDatePicker" parent="Theme.AppCompat.Light.Dialog">
        <item name="colorAccent">@color/dark_blue</item>
        <item name="android:textColorPrimary">@color/light_blue</item>
        <item name="android:textColorSecondary">@color/medium_blue</item>
    </style>

</resources>